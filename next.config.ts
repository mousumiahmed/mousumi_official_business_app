/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // enables static HTML export
  // optional: set a base path if deploying to GitHub Pages
  basePath: '/mousumi_official_business_app', 
};

module.exports = nextConfig;
